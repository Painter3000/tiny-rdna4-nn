# Public archive scope

Native binaries contained in qualified archives are retained solely as
reference and provenance artifacts. They are not intended as universally
executable distributions for other ROCm, PyTorch, compiler, or GPU
configurations. Build local modules from the archive's included sources and
build contracts.
