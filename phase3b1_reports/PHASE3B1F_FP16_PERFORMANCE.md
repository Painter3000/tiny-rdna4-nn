# Phase 3B1-F1 – Reproducible FP16 Performance

Decision: `PHASE3B1F_PERFORMANCE_GAIN_CONFIRMED`

Time and memory are reported with equal status. Batch 1024 is classified as a `latency-bound / launch-overhead-dominated regime`; batch 16384 is the throughput regime.
